type Query {
    users(id: Int) : [User]
}

type User {
    id: Int!
    userName: String
    emailId: String
    firstName: String
    lastName: String
    age: Int
    role: UserRole
}

type UserRole {
    id: Int!
    userId: Int!
    role: String
}